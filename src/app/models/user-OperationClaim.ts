export interface UserOperationClaim{
    id:number
    userId:number
    operationClaimId:number
}

export interface ListUserOperationClaim{
    id:number
    userId:number
    operationClaimIds:number[]
}